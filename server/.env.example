# PostgreSQL connection string
# Replace with your actual credentials (host, port, user, password, database)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/rice_monitoring

# Optional: API server port (default 3001)
PORT=3001
